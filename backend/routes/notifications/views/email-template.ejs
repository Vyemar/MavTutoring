<!DOCTYPE html>
<html lang="en">
 
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= subject %> - TutorTech Meetings
  </title>
  <!-- Bootstrap 5 CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
 
<body style="background-color: #f8f9fa; padding-top: 30px; padding-bottom: 30px;">
 
  <div class="container my-4">
    <div class="card shadow-lg mx-auto" style="max-width: 600px;">
      <div class="card-body">
        <h2 class="card-title text-center text-primary">Hello <%= name %>!</h2>
        <p class="card-text text-center">
          <%= message %>
        </p>
 
        <table class="table table-striped table-bordered table-primary align-middle mt-4">
          <tbody>
            <tr>
              <th class="text-center">Status</th>
            </tr>
            <tr>
              <td class="text-center fw-bold"><%= session.status %></td>
            </tr>
 
            <tr>
              <th class="text-center">On</th>
            </tr>
            <tr>
              <td class="text-center"><%= (new Date(session.sessionTime)).toLocaleString() %></td>
            </tr>
 
            <tr>
              <th class="text-center">Duration</th>
            </tr>
            <tr>
              <td class="text-center"><%= session.duration %> minute(s)</td>
            </tr>
 
            <% if (isTutor) { %>
              <tr>
                <th class="text-center">Student</th>
              </tr>
              <tr>
                <td class="text-center"><%= session.studentID.firstName %> <%= session.studentID.lastName %></td>
              </tr>
            <% } else { %>
              <tr>
                <th class="text-center">Tutor</th>
              </tr>
              <tr>
                <td class="text-center"><%= session.tutorID.firstName %> <%= session.tutorID.lastName %></td>
              </tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
  <!-- Bootstrap 5 JS Bundle (optional for any interactions) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
 
</body>
 
</html>
 